# Test fixture: a truncated unicode escape, which Properties.load rejects with
# IllegalArgumentException. Used to prove AppVersion degrades instead of failing its static
# initialiser. Not valid properties syntax on purpose.
version=\uZZZZ
