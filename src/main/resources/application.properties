# Filtered at build time by maven-resources-plugin; see the <resources> block in pom.xml.
# The value below is a Maven token and is replaced during the build — do not hand-edit it.
version=${project.version}
